# Browser-based smoke tests (tests/smoke/). Kept separate from
# requirements.txt so the production / dev install stays small —
# Playwright pulls ~150 MB of Chromium binaries on first run.
#
# Versions intentionally unpinned so CI always picks a mutually
# compatible playwright + pytest-playwright pair. If we ever need
# to lock the matrix, do it then with explicit pins.
#
# Install path:
#   pip install -r requirements-smoke.txt
#   playwright install chromium
#
# Run:
#   pytest tests/smoke/
playwright
pytest-playwright
