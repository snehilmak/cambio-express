<?xml version="1.0" encoding="utf-8"?>
<!--
  DineroBookTV manifest.

  Two key Fire TV / Android TV requirements:
    1. <intent-filter> action MAIN + category LEANBACK_LAUNCHER  →
       app appears on the Fire TV home screen.
    2. <uses-feature android:name="android.software.leanback"
                     android:required="false">  →
       allows sideload onto regular Android tablets / phones for
       testing without breaking the leanback-only contract.
    3. <uses-feature android:name="android.hardware.touchscreen"
                     android:required="false"> →
       Fire TV has no touchscreen; declaring this required would
       hide the app from the Amazon Appstore.

  android:banner is the launcher tile shown on the Fire TV home row.
  Amazon Appstore upload also asks for a 1280×720 PNG separately —
  see firetv/README.md "Amazon submission".
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:banner="@drawable/app_banner"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.DineroBookTV"
        android:usesCleartextTraffic="false"
        tools:targetApi="33">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PairingActivity"
            android:exported="false"
            android:screenOrientation="landscape" />

        <activity
            android:name=".DisplayActivity"
            android:exported="false"
            android:screenOrientation="sensorLandscape"
            android:configChanges="orientation|screenSize|keyboardHidden" />

    </application>
</manifest>
