"""Empty placeholder — the last Flask blueprint files (``errors.py``
and ``spa_cutover.py``) were removed in PR #548 (Flask-removal-3).
The directory itself goes away in the final cleanup PR alongside
``app.py`` and ``api/Flask/``."""
