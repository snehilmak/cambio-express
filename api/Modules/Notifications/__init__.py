"""Notifications module — outbound email + push helpers.

Today: trial-reminder eligibility queries + the static
subject/body templates. The Flask-bound rendering + delivery
glue (`_send_email`, `render_template`) stays in app.py for now.
"""
