"""Batches module — ACH batch CRUD for the SPA.

Lists ACH batches per store with sort + variance computation,
plus the create / edit / link-transfers endpoints the batches
page uses. All routes are under ``/api/v2/batches``.
"""
